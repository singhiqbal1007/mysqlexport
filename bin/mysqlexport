#!/usr/bin/env ruby

require_relative "../lib/mysqlexport"
require_relative "../lib/mysqlexport/cli"

cli = Mysqlexport::Cli.new
cli.parse_options

options = cli.config

puts options

Mysqlexport::Csv.new(options).to_path
